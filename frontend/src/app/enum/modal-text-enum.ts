export enum ModalTextEnum {
  titleModalDownloadFile = 'The generation of the file has started',
  bodyModalDownloadFile = 'Once finished, download the file on the history page',
  titleModalSendFile = 'File sended',
  bodyModalSendFile = 'The file was sended to Cloud and you can see the details of the transfer on the history page',
}
